export function defaultAvatar(colorCode = 1, format = 'jpg') {
  return `./img/default_avatar/${colorCode}.${format}`;
}

export function defaultProfileBanner(colorCode = 1, format = 'jpg') {
  return `./img/default_profile_banner/${colorCode}.${format}`;
}

export function defaultSpaceBanner(colorCode = 1, format = 'jpg') {
  return `./img/default_space_banner/${colorCode}.${format}`;
}
