import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.pony-house-matrix.app',
    appName: 'pony-house-matrix',
    webDir: 'dist',
    bundledWebRuntime: false
};

export default config;