// import insertMatrixAgent, { startCustomDNS } from '../../old/electron/preload/dns';

// contextBridge.exposeInMainWorld('nodeFetch', insertMatrixAgent);
// contextBridge.exposeInMainWorld('startCustomDNS', startCustomDNS);
